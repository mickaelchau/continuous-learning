En Go, les interfaces fonctionnent de maniere inverses a celles dans Java.

interface test:
- print
- taille

print est delete de l'objet:
(Java) => Erreur.
(Go) => Plus un test.

On essaie de creer l'objet:
(Java) => On declare une interface.
(Go) => Si on declare un objet avec les deux fonctions de l'interface test
        il s'agit d'un test.

any dependencies not imported is indirect
otherwise, it is direct dependency

